# Copy this file to .env and fill in real values. .env is git-ignored.
#
# SMTP relay host used to send sign-up verification emails. Must be an SMTP
# server that accepts unauthenticated mail on port 25. On the DataArt network
# this is the internal relay; substitute your own relay elsewhere.
SMTP_HOST=smtp.example.com
